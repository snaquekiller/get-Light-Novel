#!/usr/bin/env bash

PORT=15110
BASE=src
DEBUG=nuke:error webpack-dev-server \
    --history-api-fallback \
    --host 0.0.0.0 \
    --public \
    --hot \
    --inline \
    --progress \
    --colors \
    --port $PORT \
    --content-base $BASE
