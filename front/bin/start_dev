#!/usr/bin/env bash

PORT=3001
BASE=src/app
DEBUG=nuke:error webpack-dev-server \
    --history-api-fallback \
    --host 0.0.0.0 \
    --public \
    --hot \
    --inline \
    --progress \
    --colors \
    --port $PORT \
    --content-base $BASE
